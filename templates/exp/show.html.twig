{% extends 'base.html.twig' %}

{% block title %}Exp{% endblock %}

{% block body %}
    <h1>Exp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exp.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ exp.title }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ exp.location }}</td>
            </tr>
            <tr>
                <th>DateFrom</th>
                <td>{{ exp.dateFrom ? exp.dateFrom|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateTo</th>
                <td>{{ exp.dateTo ? exp.dateTo|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exp_index') }}">back to list</a>

    <a href="{{ path('exp_edit', {'id': exp.id}) }}">edit</a>

    {{ include('exp/_delete_form.html.twig') }}
{% endblock %}
